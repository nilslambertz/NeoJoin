export PerformanceTestTargetChainLength10Nested0 to "http://vitruv.tools/nilslambertz/PerformanceTestTargetChainLength10Nested0"

import "http://example.org/PerformanceTestModel"

from A a create PerformanceTestTargetChainLength10Nested0 {
    someExpression := a.refC_Multi
        .flatMap[c  | c.refE_Multi]
        .flatMap[e  | e.refG_Multi]
        .flatMap[g  | g.refI_Multi]
        .flatMap[i  | i.refA_Multi]
        .filter[a2 | a2.attA2 >= 10]
        .flatMap[a2 | a2.refI_Multi]
        .filter[i2 | i2.attI2 < 100]
        .map[i2    | i2.refJ_Single]
        .findFirst[] create PerformanceTestTargetChainLength10Nested0Child {
                             }
}
