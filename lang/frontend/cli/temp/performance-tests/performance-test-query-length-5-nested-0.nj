export PerformanceTestTargetChainLength5Nested0 to "http://vitruv.tools/nilslambertz/PerformanceTestTargetChainLength5Nested0"

import "http://example.org/PerformanceTestModel"

from A a create PerformanceTestTargetChainLength5Nested0 {
    someExpression := a.refC_Multi
        .map[c | c.refD_Single]
        .flatMap[d | d.refF_Multi]
        .filter[f | f.attF1 > 0.0]
        .findFirst[] create PerformanceTestTargetChainLength5Nested0Child {
                             }
}
